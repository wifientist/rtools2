# Environment Configuration Template
#
# USAGE:
#   Development:  cp .env.example .env
#   Production:   cp .env.example .env.production
#
# NEVER commit .env or .env.production to version control!

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Docker Compose will use these to configure PostgreSQL
# The DATABASE_URL will be automatically constructed by docker-compose
POSTGRES_DB=rtools2
POSTGRES_USER=postgres
DB_PASSWORD=generate_secure_password_here

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# For Docker Compose: REDIS_HOST/PORT are set in docker-compose.yml
# For local dev without Docker: uncomment and set these
# REDIS_HOST=localhost
# REDIS_PORT=6379
REDIS_DB=1
# Generate with: openssl rand -hex 16
REDIS_PASSWORD=generate_secure_redis_password_here

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# Generate with: openssl rand -hex 32
AUTH_SECRET_KEY=generate_random_secret_key_here
AUTH_ALGORITHM=HS256

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=generate_fernet_key_here

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Development example: http://localhost:4173,http://localhost
# Production example: https://yourdomain.com,https://api.yourdomain.com
CORS_ORIGINS=http://localhost:4173,http://localhost

# =============================================================================
# EMAIL CONFIGURATION (Choose SendGrid OR Mailgun)
# =============================================================================

# SendGrid Configuration
SMTP_SERVER=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
WEBAPI_KEY=your_sendgrid_api_key_here
SMTP_PASSWORD=your_sendgrid_password_here
FROM_EMAIL=no-reply@yourdomain.com

# Mailgun Configuration (Alternative)
MAILGUN_API_KEY=your_mailgun_api_key_here
MAILGUN_SENDING_KEY=your_mailgun_sending_key_here
MAILGUN_DOMAIN=mg.yourdomain.com

# =============================================================================
# INTEGRATION SERVICES (Optional)
# =============================================================================
INTEGRATION_API_URL=http://localhost:4175

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
VITE_API_BASE_URL=/api
VITE_ALLOWED_HOSTS=localhost

# =============================================================================
# R1 API CREDENTIALS (Optional - can be stored in database instead)
# =============================================================================
# SP_R1_DEMO_ADMIN_CLIENT_ID=
# SP_R1_DEMO_ADMIN_SHARED_SECRET=
# SP_R1_DEMO_MSP_ID=

# =============================================================================
# DEPLOYMENT NOTIFICATIONS (Optional)
# =============================================================================
# Slack webhook for deployment notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# GitHub repository URL (for commit links in notifications)
# GITHUB_REPO_URL=https://github.com/yourusername/rtools2

# API host for health checks
# API_HOST=api.ruckustools.rossho.me
