export type Proposal = {
    id: number;
    title: string;
    description: string;
    budget: number;
    location: string;
    deadline: string;
    createdBy: string; // User who posted the project
  };
  